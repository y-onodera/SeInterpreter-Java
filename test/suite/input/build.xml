<project name="test">
    <target name="nextDate">
        <tstamp>
            <format property="resultDate" pattern="yyyy/MM/dd" offset="${add_days}" unit="day"/>
        </tstamp>
    </target>
</project>